import { ExCustomPipesPipe } from './ex-custom-pipes.pipe';

describe('ExCustomPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExCustomPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
